import CreatePost from "modules/admin/post/createpost";

export const metadata = {
  title: "Create Post",
};

const CreatePostPage = () => {
  return <CreatePost />;
};

export default CreatePostPage;
