import { CreateUser } from "modules/admin/user/createuser";

export const metadata = {
  title: "Create User",
};

const CreateUserPage = () => {
  return <CreateUser />;
};

export default CreateUserPage;
